import adonis_schema_versions from 'App/Models/adonis_schema_versions'
import Factory from '@ioc:Adonis/Lucid/Factory'

export default Factory.define(adonis_schema_versions, ({ faker }) => {
  return {
    //
  }
}).build()
