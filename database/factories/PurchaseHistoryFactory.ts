import purchaseHistory from 'App/Models/purchaseHistory'
import Factory from '@ioc:Adonis/Lucid/Factory'

export default Factory.define(purchaseHistory, ({ faker }) => {
  return {
    //
  }
}).build()
