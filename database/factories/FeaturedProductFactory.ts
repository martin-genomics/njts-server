import featuredProducts from 'App/Models/featuredProducts'
import Factory from '@ioc:Adonis/Lucid/Factory'

export default Factory.define(featuredProducts, ({ faker }) => {
  return {
    //
  }
}).build()
